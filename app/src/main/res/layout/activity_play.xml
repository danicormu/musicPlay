<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.example.so.musicpower.playActivity">


    <RelativeLayout
        android:id="@+id/container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        >
    <!--    <at.markushi.ui.CircleButton
            android:id="@+id/BtnStop"
            android:onClick="stopTrace"
            android:layout_width="@dimen/little_button"
            android:layout_height="@dimen/little_button"
            android:src="@drawable/ic_stop"
            android:layout_centerVertical="true"
            android:layout_toLeftOf="@+id/btnPlayER"
            android:layout_toStartOf="@+id/btnPlayER"
            android:layout_marginTop="10dp"
            android:layout_gravity="right|bottom"
            app:cb_color="#00994C"
            app:cb_pressedRingWidth="4dp"/> -->
     <!--   <at.markushi.ui.CircleButton
            android:id="@+id/imageSee"
            android:onClick="seeRoute"
            android:layout_width="@dimen/little_button"
            android:layout_height="@dimen/little_button"
            android:layout_centerHorizontal="true"
            android:src="@drawable/ic_go_end"
            android:layout_gravity="right|bottom"
            android:layout_alignTop="@+id/BtnStop"
            android:layout_toRightOf="@+id/btnPlayER"
            android:layout_toEndOf="@+id/btnPlayER"
            android:layout_marginTop="10dp"
            app:cb_color="#00994C"
            app:cb_pressedRingWidth="4dp"/> -->
        <at.markushi.ui.CircleButton
            android:id="@+id/play_Music"
            android:layout_width="100dip"
            android:layout_height="100dip"
            android:layout_centerHorizontal="true"
            android:layout_gravity="center"
            android:src="@mipmap/ic_play"
            app:cb_color="#3F51B5"
            app:cb_pressedRingWidth="4dp" />
    </RelativeLayout>
</RelativeLayout>
